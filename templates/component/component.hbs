<script setup lang="ts">
/** props */
type {{pascalCase name}}Props = {
  /** 标题（按需增删） */
  title: string
}

/** emits */
type {{pascalCase name}}Emits = {
  (e: 'update'): void
}

const props = defineProps<{{pascalCase name}}Props>()
const emit = defineEmits<{{pascalCase name}}Emits>()
</script>

<template>
  <div class="{{kebabCase name}}">
    \{{ props.title }}
    <a-button type="primary" @click="emit('update')">触发 update</a-button>
  </div>
</template>

<style scoped lang="less">
.{{kebabCase name}} {
  // TODO: 按需补充样式
}
</style>
