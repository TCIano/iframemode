import { useAsyncState } from '@vueuse/core'

import { fetchXxxListApi } from '@/api/xxx' // TODO: 替换为实际接口

export function use{{pascalCase name}}() {
  const { error, execute, isLoading: loading, state: data } = useAsyncState(
    () => fetchXxxListApi(), // TODO: 替换为实际接口
    [], // 初始数据，替换为对应类型
  )

  function fetchData() {
    return execute()
  }

  return { data, error, fetchData, loading }
}
