import type { TApiResponse, TPageResult, TPaginationReq } from './common'

/** 模块公共基类 — 所有该模块接口继承此类型 */
type T{{pascalCase name}}Base = {
  createdAt: string
  id: number
  updatedAt: string
}

/** 单条数据 */
type T{{pascalCase name}}Info = {
  // TODO: add fields
} & T{{pascalCase name}}Base

/** 列表查询参数 */
type T{{pascalCase name}}ListReq = {
  // TODO: add filter fields
} & TPaginationReq

/** 新增请求参数 */
type T{{pascalCase name}}CreateReq = object

/** 编辑请求参数（部分字段更新） */
type T{{pascalCase name}}UpdateReq = Partial<T{{pascalCase name}}CreateReq>

/** 列表响应 */
type T{{pascalCase name}}ListResp = TApiResponse<TPageResult<T{{pascalCase name}}Info>>

export type {
  T{{pascalCase name}}Base,
  T{{pascalCase name}}CreateReq,
  T{{pascalCase name}}Info,
  T{{pascalCase name}}ListReq,
  T{{pascalCase name}}ListResp,
  T{{pascalCase name}}UpdateReq,
}
