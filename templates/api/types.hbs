import type {
  TApiResponse,
  TPageResult,
  TPaginationReq,
} from '@/types/common'

/** 模块公共基类，所有该模块接口类型继承此类型 */
type T{{pascalCase moduleName}}Base = {
  createdAt: string
  id: number
  updatedAt: string
}

/** 单条数据 */
type T{{pascalCase moduleName}}Info = T{{pascalCase moduleName}}Base & {
  // TODO: 补充业务字段
}

/** 列表查询参数 */
type T{{pascalCase moduleName}}ListReq = TPaginationReq & {
  // TODO: 补充筛选字段
}

/** 新增请求参数 */
type T{{pascalCase moduleName}}CreateReq = object

/** 编辑请求参数（部分字段更新） */
type T{{pascalCase moduleName}}UpdateReq = Partial<T{{pascalCase moduleName}}CreateReq>

/** 列表响应 */
type T{{pascalCase moduleName}}ListResp = TApiResponse<
  TPageResult<T{{pascalCase moduleName}}Info>
>

export type {
  T{{pascalCase moduleName}}Base,
  T{{pascalCase moduleName}}CreateReq,
  T{{pascalCase moduleName}}Info,
  T{{pascalCase moduleName}}ListReq,
  T{{pascalCase moduleName}}ListResp,
  T{{pascalCase moduleName}}UpdateReq,
}
