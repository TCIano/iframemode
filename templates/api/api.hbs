import type { TApiResponse } from '@/types/common'
import type {
  T{{pascalCase name}}CreateReq,
  T{{pascalCase name}}Info,
  T{{pascalCase name}}ListReq,
  T{{pascalCase name}}ListResp,
  T{{pascalCase name}}UpdateReq,
} from '@/types/{{name}}'

import request from '@/utils/request'

const PREFIX = '/{{name}}'

export function fetch{{pascalCase name}}ListApi(params: T{{pascalCase name}}ListReq) {
  return request.get<T{{pascalCase name}}ListResp>(`${PREFIX}/list`, { params })
}

export function fetch{{pascalCase name}}DetailApi(id: number) {
  return request.get<TApiResponse<T{{pascalCase name}}Info>>(`${PREFIX}/${id}`)
}

export function create{{pascalCase name}}Api(data: T{{pascalCase name}}CreateReq) {
  return request.post<TApiResponse<T{{pascalCase name}}Info>>(PREFIX, data)
}

export function update{{pascalCase name}}Api(id: number, data: T{{pascalCase name}}UpdateReq) {
  return request.put<TApiResponse<T{{pascalCase name}}Info>>(`${PREFIX}/${id}`, data)
}

export function delete{{pascalCase name}}Api(id: number) {
  return request.delete<TApiResponse>(`${PREFIX}/${id}`)
}
